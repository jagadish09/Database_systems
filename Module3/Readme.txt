I have included three files.
They are DAO java implementation file,Root configuration file and POM file because i modified for oracle database access. I included atifact and properties to access oracle database in POM.xml

We must include ojdbc6.jar into maven dependencies to execute this project.